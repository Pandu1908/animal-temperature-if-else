# animal-temperature-if-else
A simple Python program that checks an animal's temperature using an if-else conditional statement. It accepts the temperature as a floating-point value, compares it with the threshold of 39.5, and displays a high-temperature warning with an instruction to check the animal when the value is above 39.5, or a normal-temperature message otherwise.
